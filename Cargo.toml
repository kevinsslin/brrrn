[package]
name = "brrrn"
version = "0.1.0"
edition = "2021"
description = "Local token burn + cost report for Claude Code and Codex"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_json = "1"
walkdir = "2"
chrono = "0.4"

[profile.release]
lto = true
